type CityData {
  name: String!
  latitude: Float!
  longitude: Float!
}

type Judge {
  number: Int!
  judge: Boolean!
}

type Article {
  index: Int!
  article: String!
}

type Query {
  odd(number: Int!): Judge!
  even(number: Int!): Judge!
  city(id: Int): CityData!
  allCity: [CityData!]!
}

type Mutation {
  addArticle(index: Int, article: String): Article!
  postUserData(emotion: Float!, city: String!): CityData!
}